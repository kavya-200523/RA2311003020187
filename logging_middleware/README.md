# Logging Middleware
This folder contains a simple logging helper for backend APIs.

The logger prints the request method and path whenever an API request is handled. It can be used during development to check which API endpoint is being called.
